# Use this to install with CUDA 12.4 wheels (recommended for your A4000):
#   pip install --upgrade --index-url https://download.pytorch.org/whl/cu124 -r requirements.txt

# ---- Core numerics (stable, compatible pins) ----
numpy==1.26.4
scipy==1.13.1
pandas==2.2.3
scikit-learn==1.6.0

# ---- PyTorch (CUDA 12.4 build) ----
torch==2.5.1+cu124
torchvision==0.20.1+cu124
triton==3.1.0

# ---- Hugging Face stack ----
transformers==4.51.3
tokenizers==0.21.1
huggingface-hub==0.30.2
safetensors==0.5.3
sentencepiece   # no tight pin needed

# ---- Plotting / viz / manifolds ----
matplotlib==3.10.0
seaborn==0.13.2
umap-learn==0.5.7

# -----GoogleSheets & Pandas integration
gspread
gspread-dataframe

spacy
unidecode

# ---- NLP utilities ----
nltk    # for FreqDist; download corpora at runtime if needed

# ---- Misc ----
requests
ipykernel